import { magicCardStoreFeature } from './magic-card-store-feature';

describe('magicCardStoreFeature', () => {
  it('should work', () => {
    expect(magicCardStoreFeature()).toEqual('magic-card-store-feature');
  });
});
